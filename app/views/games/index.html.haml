/ %p#error
/   = flash[:error]

= link_to 'Track a new game', new_game_path, :class => 'track_new_game'

%h1 My tracked games:

- @games.each do |game|
  %tr
    %td= link_to game.name, game
    %td= game.format_time(game.created_at)
  %br